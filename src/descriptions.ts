import { IDatabase } from "./databaseConnections/interface";

const getDescription = (db: IDatabase, tableName: string) => {
  // Gather info: schema + 3 examples

  // Wrap in prompt

  // Send to open AI

  // Return description

  console.log("Getting description for table:", tableName);
  return "";
};

export { getDescription };
